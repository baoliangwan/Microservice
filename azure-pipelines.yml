# Docker
# Build and push an image to Azure Container Registry
# https://docs.microsoft.com/azure/devops/pipelines/languages/docker

trigger:
- master

resources:
- repo: self

variables:
  # Container registry service connection established during pipeline creation
  dockerRegistryServiceConnection: '1141bd92-e8ea-44d6-8f73-e42f620ed5c7'
  containerRegistry: 'servicemeshbaoliang.azurecr.io'
  tag: '$(Build.BuildId)'
  
  # Agent VM image name
  vmImageName: 'ubuntu-latest'

stages:
- stage: Build
  displayName: Build and push stage
  jobs:  
  - job: Build
    displayName: Build
    pool:
      vmImage: $(vmImageName)
    strategy:
      maxParallel: 2
      matrix:
        imageRepo_1:
          imageRepository: 'springbootmicroservicecurrencyconversion'
          dockerfilePath: '$(Build.SourcesDirectory)/$(imageRepository)/Dockerfile'
        imageRepo_2:
          imageRepository: 'springbootmicroserviceforexservice'
          dockerfilePath: '$(Build.SourcesDirectory)/$(imageRepository)/Dockerfile'
    steps:
    - task: Docker@2
      displayName: Build and push an image to container registry
      inputs:
        command: buildAndPush
        repository: $(imageRepository)
        dockerfile: $(dockerfilePath)
        containerRegistry: $(dockerRegistryServiceConnection)
        tags: |
          $(tag)
